<a href="/games/<%= game._id %>">
  <div class="game-card"> 

    <img src="<%= game.imageUrl %>" alt="<%= game.title %>">

    <p class="game-card-genre">
      <%= game.genre %>
    </p>

    <h3>
      <%= game.title %>
    </h3>

  </div>
</a>